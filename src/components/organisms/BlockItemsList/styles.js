const styles = theme => ({
  root: {
    width: "100%",
    height: "calc(100vh - 135px)",
    overflowY: "auto",
    backgroundColor: theme.palette.background.paper
  }
});

export default styles;
