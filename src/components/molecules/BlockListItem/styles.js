const styles = theme => ({
  root: {
    display: "flex",
    justifyContent: "space-between"
  },
  container: {
    display: "flex"
  },
  icon: {
    color: "rgba(0, 0, 0, 0.54);"
  },
  img: {
    width: 40,
    height: 40
  }
});

export default styles;
