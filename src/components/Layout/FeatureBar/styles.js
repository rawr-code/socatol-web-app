const colorWhite = 'rgba(255, 255, 255, 0.7)';

const styles = () => ({
  toolbar: {
    minHeight: 64,
    padding: '12px 24px 0',
    marginBottom: 4
  },
  button: {
    borderColor: colorWhite
  }
});
export default styles;
